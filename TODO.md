# TODO

- [x] Cek lokasi icon landing page (favicon di `app/[slug]/layout.tsx`, gambar di `LandingPageClient.tsx`).
- [x] Ubah favicon/tab browser dari `public/dana-logo.svg` ke `public/dana_icon.svg` di `app/[slug]/layout.tsx`.
- [ ] Tambahkan `openGraph.images` dan `twitter.images` agar sosmed ambil gambar dari `public/dana-icon.svg` tanpa mengubah UI.

